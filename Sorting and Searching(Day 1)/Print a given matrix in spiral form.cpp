#include<bits/stdc++.h>
using namespace std;
int  stockbuysell(vector<int> &stockPrice)
{
    int minval=stockPrice[0],profit=0;
    for(int i=1;i<stockPrice.size();i++)
    {
        if(minval<=stockPrice)
    }
}


#H/w -----buy two stock and sell;
